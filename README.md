# Spring-Boot-Product-CRUD-API-No-ORM-In-Memory-Storage-
Spring Boot REST API for Product CRUD operations. Implemented in-memory data storage using ArrayList without ORM (JPA/Hibernate). Supports GET, POST, PUT and DELETE endpoints. APIs tested using Postman. Focused on core REST principles and Spring Boot fundamentals.
